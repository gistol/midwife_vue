{% extends 'base.html.twig' %}

{% block stylesheets %}

{% endblock %}

{% block title %}User Profile{% endblock %}

{% block body %}

    <nav-menu first_name="{{ app.user.firstName }}" last_name="{{ app.user.lastName }}"></nav-menu>

    {% include 'flash_messages.html.twig' %}

    <user-profile username="{{ app.user.username }}" first_name="{{ app.user.firstName }}" last_name="{{ app.user.lastName }}" email="{{ app.user.email }}"></user-profile>

{% endblock %}
