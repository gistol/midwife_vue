{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block scripts %}

{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}

    {% include 'flash_messages.html.twig' %}

    {{ app.user.firstName }}
    <nav-menu firstName="{{ app.user.firstName }}" lastName="{{ app.user.lastName }}"></nav-menu>

    <div class="container-fluid">
        <hr />
        <div class="row">
            <div class="col-sm-4">
                {#Here goes the form #}

            </div>

            <div class="col-sm-8">
                <p>Tasks that are done you can find here: <a href="#">CLICK HERE </a></p>
                <div class="agenda">
                    <div class="table-responsive">
                        <table class="table table-condensed table-bordered">
                            <thead>
                            <tr>
                                <th>Date</th>
                                <th>Time</th>
                                <th>Event</th>
                                <th>Edit</th>
                                <th>Remove</th>
                            </tr>
                            </thead>
                            <tbody>
                            <!-- Single event in a single day -->
                            {% for task in tasks %}

                                {#If statement displaying ONLY UPCOIMING and today's tasks#}

                                {% if ( task.start | date("Y-m-d") ) >= "now"|date("Y-m-d") %}

                                    Displaing different background for TODAY's tasks
                                    {% if ( task.start | date("Y-m-d") ) == ( "now" |date("Y-m-d") ) %}
                                    <tr style="background-color: #e6ffb3;">
                                    {% else %}
                                        <tr>
                                    {% endif %}

                                    <td class="agenda-date" class="active" rowspan="1">
                                        <div class="dayofmonth">{{ task.start | date("d") }}</div>
                                        <div class="dayofweek">{{ task.start | date("D") }}</div>
                                        <div class="shortdate text-muted">{{ task.start | date("M") }}, {{ task.start | date("d") }}</div>
                                    </td>
                                    <td class="agenda-time">
                                        {{ task.start | date("H:s") }} - {{ task.end | date("H:s") }}
                                    </td>
                                    <td class="agenda-events">
                                        <div class="agenda-event"> 
                                            {{ task.description }}
                                        </div>
                                    </td>
                                </tr>
                                {% endif %}

                            {% endfor %}

                            {#<!-- Multiple events in a single day (note the rowspan) -->#}
                            {#<tr>#}
                                {#<td class="agenda-date" class="active" rowspan="3">#}
                                    {#<div class="dayofmonth">24</div>#}
                                    {#<div class="dayofweek">Thursday</div>#}
                                    {#<div class="shortdate text-muted">July, 2014</div>#}
                                {#</td>#}
                                {#<td class="agenda-time">#}
                                    {#8:00 - 9:00 AM#}
                                {#</td>#}
                                {#<td class="agenda-events">#}
                                    {#<div class="agenda-event">#}
                                        {#Doctor's Appointmentqqqqq#}
                                        {#eehhehhehe#}
                                        {#ehehhehe#}
                                        {#hehehe#}
                                        {#eheheh#}
                                        {#ehehhe#}
                                        {#ehh ehehhehehe#}
                                        {#nenenene nenenen nennenen#}
                                        {#en nen nnenenekwo owoe en ne e [qqp  pq enne#}
                                        {#nenenen#}
                                    {#</div>#}
                                {#</td>#}
                            {#</tr>#}
                            {#<tr>#}
                                {#<td class="agenda-time">#}
                                    {#10:15 AM - 12:00 PM#}
                                {#</td>#}
                                {#<td class="agenda-events">#}
                                    {#<div class="agenda-event">#}
                                        {#Meeting with executives#}
                                    {#</div>#}
                                {#</td>#}
                            {#</tr>#}
                            {#<tr>#}
                                {#<td class="agenda-time">#}
                                    {#7:00 - 9:00 PM#}
                                {#</td>#}
                                {#<td class="agenda-events">#}
                                    {#<div class="agenda-event">#}
                                        {#Aria's dance recital#}
                                    {#</div>#}
                                {#</td>#}
                            {#</tr>#}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        .agenda {  }

        /* Dates */
        .agenda .agenda-date { width: auto; }
        .agenda .agenda-date .dayofmonth {
            min-width: 40px;
            font-size: 36px;
            line-height: 36px;
            float: left;
            text-align: right;
            margin-right: 10px;
        }
        .agenda .agenda-date .shortdate {
            font-size: 0.75em;
        }


        /* Times */
        .agenda .agenda-time { width: auto; }


        /* Events */
        .agenda .agenda-events {  }
        .agenda .agenda-events .agenda-event {  }

        @media (max-width: 767px) {

        }


    </style>

{% endblock %}


